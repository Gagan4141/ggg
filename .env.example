# Maximum upload size in megabytes. Uploaded files are processed in memory only.
MAX_UPLOAD_MB=15
# OCR backend: auto (hosted OCR when OCR_SPACE_API_KEY is set) or none
OCR_PROVIDER=auto
# Get a free OCR.Space API key from https://ocr.space/ocrapi
OCR_SPACE_API_KEY=your_api_key_here
